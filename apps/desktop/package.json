{
  "name": "@nexos/desktop",
  "version": "0.6.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "main.cjs",
  "scripts": {
    "dev": "electron .",
    "test": "vitest run",
    "deploy:daemon": "node scripts/deploy-daemon.mjs",
    "build": "pnpm run deploy:daemon && electron-builder --win",
    "build:publish": "pnpm run deploy:daemon && electron-builder --win --publish always"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "electron-updater": "^6.8.9"
  },
  "devDependencies": {
    "electron": "^33.4.5",
    "electron-builder": "^26.15.3",
    "happy-dom": "^15.11.7",
    "vitest": "^2.1.8"
  }
}
