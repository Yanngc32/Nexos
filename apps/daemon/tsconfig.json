{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    /* scripts/ é .mjs sem tipos; allowJs deixa o tsc inferir em vez de recusar o import. */
    "allowJs": true
  },
  "include": ["src", "test", "scripts"]
}
